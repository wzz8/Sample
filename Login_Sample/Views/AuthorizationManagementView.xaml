<UserControl x:Class="Login_Sample.Views.AuthorizationManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Key" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="授权管理" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：授权操作 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="授权操作" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <Button Content="生成授权码" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding GenerateAuthorizationCodeCommand}"/>
                            <Button Content="验证授权码" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding VerifyAuthorizationCodeCommand}"/>
                            <Button Content="查看授权状态" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding CheckAuthorizationStatusCommand}"/>
                            <Button Content="导出授权信息" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding ExportAuthorizationInfoCommand}"/>
                        </StackPanel>
                    </materialDesign:Card>
                    
                    <TextBlock Text="授权状态" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,20,0,10"/>
                    <ListBox Margin="10">
                        <ListBoxItem>已授权</ListBoxItem>
                        <ListBoxItem>未授权</ListBoxItem>
                        <ListBoxItem>授权过期</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>
            
            <!-- 右侧：授权列表和详情 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="授权列表" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <!-- 搜索和筛选 -->
                    <StackPanel Orientation="Horizontal" Margin="15,0,15,15">
                        <TextBox Width="200" materialDesign:HintAssist.Hint="授权码" Margin="0,0,10,0"/>
                        <TextBox Width="200" materialDesign:HintAssist.Hint="电脑名称" Margin="0,0,10,0"/>
                        <ComboBox Width="150" materialDesign:HintAssist.Hint="授权状态" Margin="0,0,10,0">
                            <ComboBoxItem Content="全部"/>
                            <ComboBoxItem Content="已授权"/>
                            <ComboBoxItem Content="未授权"/>
                            <ComboBoxItem Content="授权过期"/>
                        </ComboBox>
                        <Button Content="搜索" Background="#3498DB" Foreground="White"/>
                        <Button Content="刷新" Background="#95A5A6" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                    
                    <!-- 授权列表 -->
                    <DataGrid AutoGenerateColumns="False" Margin="15,0,15,15">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="授权ID" Width="100"/>
                            <DataGridTextColumn Header="授权码" Width="*"/>
                            <DataGridTextColumn Header="电脑名称" Width="150"/>
                            <DataGridTextColumn Header="MAC地址" Width="180"/>
                            <DataGridTextColumn Header="授权日期" Width="120"/>
                            <DataGridTextColumn Header="过期日期" Width="120"/>
                            <DataGridTextColumn Header="状态" Width="100"/>
                            <DataGridTemplateColumn Header="操作" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="详情" Background="#3498DB" Foreground="White" Width="60"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- 授权统计信息 -->
                    <TextBlock Text="授权统计信息" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,0,0,10"/>
                    <Border Background="#F8F9FA" Margin="15,0,15,15" Height="200" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                        <StackPanel Margin="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="0">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="总授权数" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="50" FontSize="24" FontWeight="Bold" Foreground="#3498DB"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="1">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="已授权" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="45" FontSize="24" FontWeight="Bold" Foreground="#27AE60"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="2">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="未授权" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="3" FontSize="24" FontWeight="Bold" Foreground="#E74C3C"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="3">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="授权过期" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="2" FontSize="24" FontWeight="Bold" Foreground="#F39C12"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>