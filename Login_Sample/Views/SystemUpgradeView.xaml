<UserControl x:Class="Login_Sample.Views.SystemUpgradeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Update" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="系统升级" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：升级操作 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="升级操作" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <TextBlock Text="当前版本：1.0.0.0" FontSize="14" Margin="0,0,0,10"/>
                            <TextBlock Text="最新版本：1.0.1.0" FontSize="14" Margin="0,0,0,10"/>
                            <TextBlock Text="升级说明：" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                            <TextBlock Text="1. 修复了系统bug" FontSize="14" Margin="10,0,0,5"/>
                            <TextBlock Text="2. 优化了系统性能" FontSize="14" Margin="10,0,0,5"/>
                            <TextBlock Text="3. 添加了新功能" FontSize="14" Margin="10,0,0,5"/>
                            <Button Content="检查更新" Background="#3498DB" Foreground="White" Margin="0,15,0,5"/>
                            <Button Content="立即升级" Background="#27AE60" Foreground="White" Margin="0,5,0,5"/>
                            <Button Content="取消升级" Background="#E74C3C" Foreground="White" Margin="0,5,0,5"/>
                        </StackPanel>
                    </materialDesign:Card>
                    
                    <TextBlock Text="升级状态" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,20,0,10"/>
                    <ListBox Margin="10">
                        <ListBoxItem>可升级</ListBoxItem>
                        <ListBoxItem>最新版本</ListBoxItem>
                        <ListBoxItem>升级中</ListBoxItem>
                        <ListBoxItem>升级失败</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>
            
            <!-- 右侧：升级记录 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="升级记录" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <!-- 搜索和筛选 -->
                    <StackPanel Orientation="Horizontal" Margin="15,0,15,15">
                        <TextBox Width="200" materialDesign:HintAssist.Hint="版本号" Margin="0,0,10,0"/>
                        <TextBox Width="200" materialDesign:HintAssist.Hint="升级日期" Margin="0,0,10,0"/>
                        <ComboBox Width="150" materialDesign:HintAssist.Hint="升级状态" Margin="0,0,10,0">
                            <ComboBoxItem Content="全部"/>
                            <ComboBoxItem Content="成功"/>
                            <ComboBoxItem Content="失败"/>
                        </ComboBox>
                        <Button Content="搜索" Background="#3498DB" Foreground="White"/>
                        <Button Content="刷新" Background="#95A5A6" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                    
                    <!-- 升级记录列表 -->
                    <DataGrid AutoGenerateColumns="False" Margin="15,0,15,15">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="版本号" Width="120" Binding="{Binding Version}"/>
                            <DataGridTextColumn Header="升级日期" Width="150" Binding="{Binding UpgradeDate}"/>
                            <DataGridTextColumn Header="升级状态" Width="100" Binding="{Binding Status}"/>
                            <DataGridTextColumn Header="升级人" Width="100" Binding="{Binding Upgrader}"/>
                            <DataGridTextColumn Header="升级说明" Width="*" Binding="{Binding Description}"/>
                            <DataGridTemplateColumn Header="操作" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="详情" Background="#3498DB" Foreground="White" Width="60"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- 升级进度 -->
                    <TextBlock Text="升级进度" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,20,0,10"/>
                    <Border Background="#F8F9FA" Margin="15,0,15,15" Height="200" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                        <StackPanel Margin="15">
                            <ProgressBar Value="75" Height="20" Background="#E0E0E0" Foreground="#3498DB" Margin="0,0,0,15"/>
                            <TextBlock Text="升级进度：75%" FontSize="14" Margin="0,0,0,10"/>
                            <TextBlock Text="当前操作：正在下载升级包" FontSize="14" Margin="0,0,0,10"/>
                            <TextBlock Text="预计剩余时间：30秒" FontSize="14" Margin="0,0,0,10"/>
                            <Button Content="取消升级" Background="#E74C3C" Foreground="White" Width="100"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
