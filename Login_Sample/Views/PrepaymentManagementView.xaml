<UserControl x:Class="Login_Sample.Views.PrepaymentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:viewModels="clr-namespace:Login_Sample.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:PrepaymentManagementViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Grid Grid.Row="0" Background="#2C3E50" Height="60">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="CreditCard" Width="28" Height="28" Foreground="White"/>
                <TextBlock Text="预收预付" FontSize="20" FontWeight="Bold" Foreground="White" Margin="15,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- 主要内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 登记和搜索区域 -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>

                <!-- 登记区域 -->
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="客户名称:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="类型:" Margin="0,0,0,5"/>
                        <ComboBox Height="30" SelectedValue="{Binding PrepaymentType}">
                            <ComboBoxItem Content="预收"/>
                            <ComboBoxItem Content="预付"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="金额:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                        <TextBlock Text="备注:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4" VerticalAlignment="Bottom">
                        <Button Command="{Binding RegisterPrepaymentCommand}" Width="100" Height="30">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" Margin="0,0,5,0"/>
                                <TextBlock Text="登记"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- 搜索区域 -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="搜索:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SearchKey, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        <Button Command="{Binding SearchRecordsCommand}" Margin="0,0,10,0" Width="80" Height="30">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Magnify" Width="16" Height="16" Margin="0,0,5,0"/>
                                <TextBlock Text="搜索"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding RefreshRecordsCommand}" Width="80" Height="30">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,5,0"/>
                                <TextBlock Text="刷新"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- 错误提示 -->
            <Grid Grid.Row="1" Background="#FEEFB3" Visibility="{Binding ErrorVisibility, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,0,10" Height="40">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <materialDesign:PackIcon Kind="Alert" Width="20" Height="20" Foreground="#9F6000"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#9F6000" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- 数据表格和操作按钮 -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <!-- 数据表格 -->
                <DataGrid Grid.Row="0" ItemsSource="{Binding PrepaymentRecords}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                          SelectedItem="{Binding SelectedRecord}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="客户名称" Binding="{Binding CustomerName}" Width="150"/>
                        <DataGridTextColumn Header="类型" Binding="{Binding PrepaymentType}" Width="80"/>
                        <DataGridTextColumn Header="金额" Binding="{Binding Amount}" Width="100"/>
                        <DataGridTextColumn Header="记录日期" Binding="{Binding RecordDate}" Width="120"/>
                        <DataGridTextColumn Header="操作人" Binding="{Binding Operator}" Width="100"/>
                        <DataGridTextColumn Header="备注" Binding="{Binding Remarks}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 操作按钮 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Command="{Binding DeleteRecordCommand}" Width="100" Height="30">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="删除"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- 加载指示器 -->
        <Grid Grid.Row="0" Grid.RowSpan="2" Background="#BBFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Loading" Width="60" Height="60" Foreground="#3498DB"/>
                <TextBlock Text="处理中..." FontSize="16" Margin="0,10,0,0" Foreground="#3498DB"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>