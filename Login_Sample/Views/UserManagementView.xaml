<UserControl x:Class="Login_Sample.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Account" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="用户管理" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：用户操作 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="用户操作" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <Button Content="增加用户" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding AddUserCommand}"/>
                            <Button Content="删除用户" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding DeleteUserCommand}"/>
                            <Button Content="启用用户" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding EnableUserCommand}"/>
                            <Button Content="注销用户" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding DisableUserCommand}"/>
                        </StackPanel>
                    </materialDesign:Card>
                    
                    <TextBlock Text="用户状态" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,20,0,10"/>
                    <ListBox Margin="10">
                        <ListBoxItem>启用</ListBoxItem>
                        <ListBoxItem>注销</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>
            
            <!-- 右侧：用户列表和详情 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="用户列表" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <!-- 搜索和筛选 -->
                    <StackPanel Orientation="Horizontal" Margin="15,0,15,15">
                        <TextBox Width="200" materialDesign:HintAssist.Hint="用户名" Margin="0,0,10,0"/>
                        <TextBox Width="200" materialDesign:HintAssist.Hint="姓名" Margin="0,0,10,0"/>
                        <ComboBox Width="150" materialDesign:HintAssist.Hint="状态" Margin="0,0,10,0">
                            <ComboBoxItem Content="全部"/>
                            <ComboBoxItem Content="启用"/>
                            <ComboBoxItem Content="注销"/>
                        </ComboBox>
                        <Button Content="搜索" Background="#3498DB" Foreground="White"/>
                        <Button Content="刷新" Background="#95A5A6" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                    
                    <!-- 用户列表 -->
                    <DataGrid AutoGenerateColumns="False" Margin="15,0,15,15">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="用户ID" Width="100"/>
                            <DataGridTextColumn Header="用户名" Width="120"/>
                            <DataGridTextColumn Header="姓名" Width="120"/>
                            <DataGridTextColumn Header="角色" Width="120"/>
                            <DataGridTextColumn Header="联系电话" Width="150"/>
                            <DataGridTextColumn Header="创建日期" Width="120"/>
                            <DataGridTextColumn Header="最后登录" Width="150"/>
                            <DataGridTextColumn Header="状态" Width="80"/>
                            <DataGridTemplateColumn Header="操作" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="详情" Background="#3498DB" Foreground="White" Width="60"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- 用户统计信息 -->
                    <TextBlock Text="用户统计信息" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,0,0,10"/>
                    <Border Background="#F8F9FA" Margin="15,0,15,15" Height="200" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                        <StackPanel Margin="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="0">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="总用户数" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="50" FontSize="24" FontWeight="Bold" Foreground="#3498DB"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="1">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="启用用户" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="45" FontSize="24" FontWeight="Bold" Foreground="#27AE60"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="2">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="注销用户" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="5" FontSize="24" FontWeight="Bold" Foreground="#E74C3C"/>
                                    </StackPanel>
                                </Border>
                                <Border Background="#FFFFFF" Margin="5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Grid.Column="3">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                                        <TextBlock Text="今日登录" FontSize="14" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                                        <TextBlock Text="12" FontSize="24" FontWeight="Bold" Foreground="#F39C12"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>