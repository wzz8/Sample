<UserControl x:Class="Login_Sample.Views.AnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="ChartPie" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="分析" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：分析类型选择 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="分析类型" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <Button Content="维修费用分析" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding NavigateToRepairCostAnalysisCommand}"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Border>
            
            <!-- 右侧：分析内容区域 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="分析内容" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <!-- 维修费用分析区域 -->
                    <Border Background="#F8F9FA" Margin="10" Height="500" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                        <StackPanel Margin="20">
                            <TextBlock Text="维修费用分析" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>
                            
                            <!-- 筛选条件 -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                <TextBlock Text="车系：" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <ComboBox Width="150" Margin="0,0,20,0">
                                    <ComboBoxItem Content="所有车系"/>
                                    <ComboBoxItem Content="奥迪"/>
                                    <ComboBoxItem Content="宝马"/>
                                    <ComboBoxItem Content="奔驰"/>
                                    <ComboBoxItem Content="大众"/>
                                </ComboBox>
                                <TextBlock Text="开始日期：" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <DatePicker Width="150" Margin="0,0,20,0"/>
                                <TextBlock Text="结束日期：" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <DatePicker Width="150" Margin="0,0,20,0"/>
                                <Button Content="分析" Background="#3498DB" Foreground="White"/>
                            </StackPanel>
                            
                            <!-- 分析结果 -->
                            <TextBlock Text="分析结果" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                            <DataGrid AutoGenerateColumns="False" Margin="0,0,0,20">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="车系" Width="100"/>
                                    <DataGridTextColumn Header="维修项目" Width="150"/>
                                    <DataGridTextColumn Header="维修次数" Width="100"/>
                                    <DataGridTextColumn Header="平均费用" Width="100"/>
                                    <DataGridTextColumn Header="总费用" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            
                            <!-- 图表区域 -->
                            <TextBlock Text="费用分布" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                            <Border Background="#FFFFFF" Height="200" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                                <TextBlock Text="图表显示区域" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>