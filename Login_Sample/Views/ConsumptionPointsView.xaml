<UserControl x:Class="Login_Sample.Views.ConsumptionPointsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:viewModels="clr-namespace:Login_Sample.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ConsumptionPointsViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Grid Grid.Row="0" Background="#2C3E50" Height="60">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Star" Width="28" Height="28" Foreground="White"/>
                <TextBlock Text="消费积分" FontSize="20" FontWeight="Bold" Foreground="White" Margin="15,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- 主要内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 搜索条件区域 -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="客户名称:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="车牌号:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding LicensePlate, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="开始日期:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="0,0,10,0">
                    <TextBlock Text="结束日期:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EndDate, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Command="{Binding SearchRecordsCommand}" Margin="0,0,10,0" Width="80" Height="30">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Magnify" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="搜索"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding RefreshRecordsCommand}" Width="80" Height="30">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="刷新"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 错误提示 -->
            <Grid Grid.Row="1" Background="#FEEFB3" Visibility="{Binding ErrorVisibility, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,0,10" Height="40">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <materialDesign:PackIcon Kind="Alert" Width="20" Height="20" Foreground="#9F6000"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#9F6000" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- 数据表格 -->
            <DataGrid Grid.Row="2" ItemsSource="{Binding ConsumptionPointRecords}" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      SelectedItem="{Binding SelectedRecord}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="客户名称" Binding="{Binding CustomerName}" Width="150"/>
                    <DataGridTextColumn Header="车牌号" Binding="{Binding LicensePlate}" Width="100"/>
                    <DataGridTextColumn Header="结算单号" Binding="{Binding SettlementNo}" Width="150"/>
                    <DataGridTextColumn Header="消费日期" Binding="{Binding ConsumptionDate}" Width="120"/>
                    <DataGridTextColumn Header="使用积分" Binding="{Binding PointsUsed}" Width="100"/>
                    <DataGridTextColumn Header="兑换金额" Binding="{Binding AmountConverted}" Width="100"/>
                    <DataGridTextColumn Header="剩余积分" Binding="{Binding BalancePoints}" Width="100"/>
                    <DataGridTextColumn Header="操作人" Binding="{Binding Operator}" Width="100"/>
                    <DataGridTextColumn Header="备注" Binding="{Binding Remarks}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- 加载指示器 -->
        <Grid Grid.Row="0" Grid.RowSpan="2" Background="#BBFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Loading" Width="60" Height="60" Foreground="#3498DB"/>
                <TextBlock Text="处理中..." FontSize="16" Margin="0,10,0,0" Foreground="#3498DB"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>