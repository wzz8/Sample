<UserControl x:Class="Login_Sample.Views.SparePartsCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="BookOpenPageVariant" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="备件目录" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 查询条件区域 -->
        <Grid Grid.Row="1" Margin="10,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Text="备件车型：" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBox Grid.Column="1" Grid.Row="0" Height="30" Margin="0,0,30,0" Text="{Binding SparePartVehicleType, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Column="2" Grid.Row="0" Text="备件编号：" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBox Grid.Column="3" Grid.Row="0" Height="30" Margin="0,0,30,0" Text="{Binding SparePartNumber, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Grid.Column="4" Grid.Row="0" Text="备件名称：" VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBox Grid.Column="5" Grid.Row="0" Height="30" Margin="0,0,30,0" Text="{Binding SparePartName, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="6" Grid.Row="0" Content="查询" Command="{Binding RefreshCommand}" Height="30" Width="80" Margin="0,0,10,0" Background="#3D6DCC" Foreground="White" />
            <Button Grid.Column="7" Grid.Row="0" Content="重置" Command="{Binding ResetCommand}" Height="30" Width="80" Background="#6C757D" Foreground="White" />
        </Grid>
        
        <!-- 备件目录数据表格 -->
        <materialDesign:Card Grid.Row="2" Margin="16" Padding="16">
            <DataGrid ItemsSource="{Binding CatalogItems}" AutoGenerateColumns="False"
                      SelectedItem="{Binding SelectedItem}"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      IsReadOnly="True"
                      RowHeight="32"
                      materialDesign:DataGridAssist.CellPadding="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="备件名称" Binding="{Binding ItemName}" Width="*"/>
                    <DataGridTextColumn Header="备件编号" Binding="{Binding ItemNumber}" Width="150"/>
                    <DataGridTextColumn Header="供应商" Binding="{Binding Supplier}" Width="150"/>
                    <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice, StringFormat=N2}" Width="100"/>
                    <DataGridTextColumn Header="仓库位置" Binding="{Binding WarehouseLocation}" Width="150"/>
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="最后更新" Binding="{Binding LastUpdatedTime, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>