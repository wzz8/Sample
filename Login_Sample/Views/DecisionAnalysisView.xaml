<UserControl x:Class="Login_Sample.Views.DecisionAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="ChartLine" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="决策分析" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：查询条件 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="查询条件" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <TextBlock Text="统计查询" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <ComboBox materialDesign:HintAssist.Hint="选择统计类型" Margin="0,0,0,10">
                                <ComboBoxItem Content="业务接待统计"/>
                                <ComboBoxItem Content="财务收支统计"/>
                                <ComboBoxItem Content="备件消耗统计"/>
                            </ComboBox>
                            <DatePicker materialDesign:HintAssist.Hint="开始日期" Margin="0,0,0,10"/>
                            <DatePicker materialDesign:HintAssist.Hint="结束日期" Margin="0,0,0,15"/>
                            
                            <Button Content="生成报表" Background="#3498DB" Foreground="White" Margin="0,10,0,0"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Border>
            
            <!-- 右侧：统计结果 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="统计结果" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <!-- 图表区域 -->
                    <Border Background="#F8F9FA" Margin="10" Height="300" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                        <TextBlock Text="图表显示区域" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#7F8C8D"/>
                    </Border>
                    
                    <!-- 数据表格 -->
                    <DataGrid AutoGenerateColumns="False" Margin="10" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="项目" Width="*"/>
                            <DataGridTextColumn Header="数量" Width="100"/>
                            <DataGridTextColumn Header="金额" Width="100"/>
                            <DataGridTextColumn Header="占比" Width="100"/>
                        </DataGrid.Columns>
                        <!-- 移除直接的数据定义，稍后通过ViewModel绑定 -->
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>