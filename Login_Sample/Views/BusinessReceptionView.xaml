<UserControl x:Class="Login_Sample.Views.BusinessReceptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="AccountGroup" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="业务接待" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>

        <!-- 模块内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 统计卡片区域 -->
                <Grid Margin="15,15,15,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 总客户数卡片 -->
                    <materialDesign:Card Grid.Column="0" Margin="0,0,15,0" Padding="20" MouseDown="Card_MouseDown" Tag="CustomerManagement">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <materialDesign:PackIcon Kind="AccountGroup" Width="40" Height="40" Foreground="#3498DB"/>
                            </StackPanel>
                            <TextBlock Text="总客户数" FontSize="14" Foreground="#7F8C8D" Margin="0,10,0,0"/>
                            <TextBlock Text="{Binding TotalCustomers}" FontSize="36" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <materialDesign:PackIcon Kind="ArrowUp" Width="16" Height="16" Foreground="#2ECC71"/>
                                <TextBlock Text="12.5% 较上月" FontSize="12" Foreground="#2ECC71"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- 今日预约数卡片 -->
                    <materialDesign:Card Grid.Column="1" Margin="0,0,15,0" Padding="20" MouseDown="Card_MouseDown" Tag="CustomerAppointment">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <materialDesign:PackIcon Kind="CalendarClock" Width="40" Height="40" Foreground="#E67E22"/>
                            </StackPanel>
                            <TextBlock Text="今日预约" FontSize="14" Foreground="#7F8C8D" Margin="0,10,0,0"/>
                            <TextBlock Text="{Binding TodayAppointments}" FontSize="36" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <materialDesign:PackIcon Kind="ArrowUp" Width="16" Height="16" Foreground="#2ECC71"/>
                                <TextBlock Text="8.3% 较昨日" FontSize="12" Foreground="#2ECC71"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- 待处理车辆卡片 -->
                    <materialDesign:Card Grid.Column="2" Margin="0,0,15,0" Padding="20" MouseDown="Card_MouseDown" Tag="VehicleReception">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <materialDesign:PackIcon Kind="CarRepair" Width="40" Height="40" Foreground="#E74C3C"/>
                            </StackPanel>
                            <TextBlock Text="待处理车辆" FontSize="14" Foreground="#7F8C8D" Margin="0,10,0,0"/>
                            <TextBlock Text="{Binding PendingVehicles}" FontSize="36" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <materialDesign:PackIcon Kind="ArrowDown" Width="16" Height="16" Foreground="#E74C3C"/>
                                <TextBlock Text="2.1% 较昨日" FontSize="12" Foreground="#E74C3C"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- 已完成服务卡片 -->
                    <materialDesign:Card Grid.Column="3" Padding="20" MouseDown="Card_MouseDown" Tag="CompletedServices">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <materialDesign:PackIcon Kind="CheckCircle" Width="40" Height="40" Foreground="#2ECC71"/>
                            </StackPanel>
                            <TextBlock Text="已完成服务" FontSize="14" Foreground="#7F8C8D" Margin="0,10,0,0"/>
                            <TextBlock Text="{Binding CompletedServices}" FontSize="36" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <materialDesign:PackIcon Kind="ArrowUp" Width="16" Height="16" Foreground="#2ECC71"/>
                                <TextBlock Text="15.2% 较昨日" FontSize="12" Foreground="#2ECC71"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>
                </Grid>

                <!-- 功能卡片区域 -->
                <Grid Margin="15,25,15,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 客户预约管理 -->
                    <materialDesign:Card Grid.Column="0" Margin="0,0,15,0" Padding="20" MouseDown="Card_MouseDown" Tag="CustomerAppointment">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="CalendarCheck" Width="60" Height="60" Foreground="#3498DB"/>
                            </StackPanel>
                            <TextBlock Text="客户预约管理" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                            <TextBlock Text="管理客户预约信息" FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="ChevronRight" Width="16" Height="16" Foreground="#95A5A6"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- 车辆接待登记 -->
                    <materialDesign:Card Grid.Column="1" Margin="0,0,15,0" Padding="20" MouseDown="Card_MouseDown" Tag="VehicleReception">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="CarClock" Width="60" Height="60" Foreground="#E67E22"/>
                            </StackPanel>
                            <TextBlock Text="车辆接待登记" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                            <TextBlock Text="登记到店车辆信息" FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="ChevronRight" Width="16" Height="16" Foreground="#95A5A6"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- 维修安排 -->
                    <materialDesign:Card Grid.Column="2" Margin="0,0,15,0" Padding="20" MouseDown="Card_MouseDown" Tag="RepairArrangement">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="Wrench" Width="60" Height="60" Foreground="#9B59B6"/>
                            </StackPanel>
                            <TextBlock Text="维修安排" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                            <TextBlock Text="安排车辆维修任务" FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="ChevronRight" Width="16" Height="16" Foreground="#95A5A6"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>

                    <!-- 客户反馈管理 -->
                    <materialDesign:Card Grid.Column="3" Padding="20" MouseDown="Card_MouseDown" Tag="CustomerFeedback">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="CommentText" Width="60" Height="60" Foreground="#1ABC9C"/>
                            </StackPanel>
                            <TextBlock Text="客户反馈管理" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,15,0,0" HorizontalAlignment="Center"/>
                            <TextBlock Text="处理客户反馈信息" FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="ChevronRight" Width="16" Height="16" Foreground="#95A5A6"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>
                </Grid>

                <!-- 快速操作区域 -->
                <materialDesign:Card Margin="15,0,15,15" Padding="20">
                    <StackPanel>
                        <TextBlock Text="快速操作" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Margin="0,0,15,0" Height="50" Background="#3498DB" Foreground="White" materialDesign:ButtonAssist.CornerRadius="4">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <materialDesign:PackIcon Kind="Plus" Width="24" Height="24" Margin="0,0,10,0"/>
                                    <TextBlock Text="新增预约"/>
                                </StackPanel>
                            </Button>

                            <Button Grid.Column="1" Margin="0,0,15,0" Height="50" Background="#E67E22" Foreground="White" materialDesign:ButtonAssist.CornerRadius="4">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <materialDesign:PackIcon Kind="Car3Plus" Width="24" Height="24" Margin="0,0,10,0"/>
                                    <TextBlock Text="车辆接待"/>
                                </StackPanel>
                            </Button>

                            <Button Grid.Column="2" Height="50" Background="#2ECC71" Foreground="White" materialDesign:ButtonAssist.CornerRadius="4">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <materialDesign:PackIcon Kind="FileDocumentEdit" Width="24" Height="24" Margin="0,0,10,0"/>
                                    <TextBlock Text="开单记录"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>