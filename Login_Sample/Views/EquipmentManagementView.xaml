<UserControl x:Class="Login_Sample.Views.EquipmentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Tools" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="设备管理" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：设备操作 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="设备操作" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <Button Content="设备登记" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding RegisterEquipmentCommand}"/>
                            <Button Content="设备修理" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding RepairEquipmentCommand}"/>
                            <Button Content="设备维护" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding MaintainEquipmentCommand}"/>
                            <Button Content="设备报废" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding ScrapEquipmentCommand}"/>
                            <Button Content="设备借用" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding BorrowEquipmentCommand}"/>
                            <Button Content="设备归还" Background="#3498DB" Foreground="White" Margin="0,5,0,5" Command="{Binding ReturnEquipmentCommand}"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Border>
            
            <!-- 右侧：设备列表和详情 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="设备列表" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <!-- 搜索和筛选 -->
                    <StackPanel Orientation="Horizontal" Margin="15,0,15,15">
                        <TextBox Width="200" materialDesign:HintAssist.Hint="设备名称" Margin="0,0,10,0"/>
                        <ComboBox Width="150" materialDesign:HintAssist.Hint="设备状态" Margin="0,0,10,0">
                            <ComboBoxItem Content="全部"/>
                            <ComboBoxItem Content="正常"/>
                            <ComboBoxItem Content="修理中"/>
                            <ComboBoxItem Content="维护中"/>
                            <ComboBoxItem Content="已报废"/>
                            <ComboBoxItem Content="已借出"/>
                        </ComboBox>
                        <Button Content="搜索" Background="#3498DB" Foreground="White"/>
                        <Button Content="刷新" Background="#95A5A6" Foreground="White" Margin="10,0,0,0"/>
                    </StackPanel>
                    
                    <!-- 设备列表 -->
                    <DataGrid AutoGenerateColumns="False" Margin="15,0,15,15">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="设备编号" Width="100"/>
                            <DataGridTextColumn Header="设备名称" Width="150"/>
                            <DataGridTextColumn Header="型号" Width="100"/>
                            <DataGridTextColumn Header="状态" Width="80"/>
                            <DataGridTextColumn Header="负责人" Width="100"/>
                            <DataGridTextColumn Header="位置" Width="150"/>
                            <DataGridTextColumn Header="购买日期" Width="120"/>
                            <DataGridTemplateColumn Header="操作" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="详情" Background="#3498DB" Foreground="White" Width="60"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- 设备使用情况跟踪 -->
                    <TextBlock Text="设备使用情况跟踪" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,0,0,10"/>
                    <Border Background="#F8F9FA" Margin="15,0,15,15" Height="200" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                        <StackPanel Margin="15">
                            <TextBlock Text="最近操作记录" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                            <ListBox>
                                <ListBoxItem>设备A - 维护 - 2026-01-24 - 张三</ListBoxItem>
                                <ListBoxItem>设备B - 修理 - 2026-01-23 - 李四</ListBoxItem>
                                <ListBoxItem>设备C - 借用 - 2026-01-22 - 王五</ListBoxItem>
                                <ListBoxItem>设备D - 归还 - 2026-01-21 - 赵六</ListBoxItem>
                                <ListBoxItem>设备E - 报废 - 2026-01-20 - 钱七</ListBoxItem>
                            </ListBox>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>