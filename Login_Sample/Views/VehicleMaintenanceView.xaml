<UserControl x:Class="Login_Sample.Views.VehicleMaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="CarRepair" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="车辆维修" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>

        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：维修表单 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="维修登记" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>

                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <TextBlock Text="车辆信息" FontWeight="Bold" Margin="0,0,0,10"/>

                            <TextBox materialDesign:HintAssist.Hint="车牌号码" Margin="0,0,0,10"/>
                            <TextBox materialDesign:HintAssist.Hint="车辆型号" Margin="0,0,0,10"/>
                            <TextBox materialDesign:HintAssist.Hint="行驶里程" Margin="0,0,0,10"/>
                            <TextBox materialDesign:HintAssist.Hint="车主姓名" Margin="0,0,0,15"/>

                            <TextBlock Text="维修信息" FontWeight="Bold" Margin="0,0,0,10"/>
                            <ComboBox materialDesign:HintAssist.Hint="维修类型" Margin="0,0,0,10">
                                <ComboBoxItem Content="常规保养"/>
                                <ComboBoxItem Content="发动机维修"/>
                                <ComboBoxItem Content="电气系统维修"/>
                                <ComboBoxItem Content="车身维修"/>
                            </ComboBox>
                            <TextBox materialDesign:HintAssist.Hint="故障描述" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,0,0,15"/>
                            <TextBox materialDesign:HintAssist.Hint="维修说明" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Margin="0,0,0,15"/>

                            <Button Content="提交维修"  Background="#3498DB" Foreground="White" Margin="0,10,0,0"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Border>

            <!-- 右侧：维修记录列表 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="维修记录" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>

                    <DataGrid AutoGenerateColumns="False" Margin="10" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="维修编号" Width="100"/>
                            <DataGridTextColumn Header="车牌号码" Width="100"/>
                            <DataGridTextColumn Header="车主姓名" Width="100"/>
                            <DataGridTextColumn Header="维修类型" Width="120"/>
                            <DataGridTextColumn Header="进厂时间" Width="150"/>
                            <DataGridTextColumn Header="预计完工" Width="150"/>
                            <DataGridTextColumn Header="状态" Width="100"/>
                            <DataGridTemplateColumn Header="操作" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="查看"  Margin="2" />
                                            <Button Content="编辑"  Margin="2" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <!-- 移除直接的数据定义，稍后通过ViewModel绑定 -->
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>