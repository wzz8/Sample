<UserControl x:Class="Login_Sample.Views.SalesSettlementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:vm="clr-namespace:Login_Sample.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converter="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Background="#F5F5F5">
    <UserControl.Resources>
        <converter:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1F618D"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="35"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Height" Value="35"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="销售结算" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Left"/>
        
        <!-- 检索条件 -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Padding="15" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 购货单位 -->
                <Label Grid.Row="0" Grid.Column="0" Content="购货单位:" Margin="0,5,10,5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5,20,5" Text="{Binding PurchaseUnit, UpdateSourceTrigger=PropertyChanged}"/>
                
                <!-- 销售单号 -->
                <Label Grid.Row="0" Grid.Column="2" Content="销售单号:" Margin="0,5,10,5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3" Margin="0,5,20,5" Text="{Binding SalesOrderNumber, UpdateSourceTrigger=PropertyChanged}"/>
                
                <!-- 销售日期 -->
                <Label Grid.Row="0" Grid.Column="4" Content="销售日期:" Margin="0,5,10,5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="5" Margin="0,5,20,5" Text="{Binding SalesDate, UpdateSourceTrigger=PropertyChanged}"/>
                
                <!-- 检索按钮 -->
                <Button Grid.Row="0" Grid.Column="6" Content="检索" Command="{Binding SearchCommand}"/>
            </Grid>
        </Border>
        
        <!-- 错误提示 -->
        <TextBlock Grid.Row="2" Margin="0,10,0,10" Foreground="Red" TextAlignment="Left" FontSize="14" Visibility="{Binding ErrorVisibility}" Text="{Binding ErrorMessage}"/>
        
        <!-- 销售单信息 -->
        <Border Grid.Row="3" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Padding="15" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 总金额 -->
                <Label Grid.Row="0" Grid.Column="0" Content="总金额:" Margin="0,5,10,5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5,20,5" Text="{Binding TotalAmount, StringFormat=N2}" IsReadOnly="True" Background="#F5F5F5"/>
                
                <!-- 折扣 -->
                <Label Grid.Row="0" Grid.Column="2" Content="折扣:" Margin="0,5,10,5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="3" Margin="0,5,20,5" Text="{Binding Discount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
                
                <!-- 实收金额 -->
                <Label Grid.Row="0" Grid.Column="4" Content="实收金额:" Margin="0,5,10,5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="5" Margin="0,5,20,5" Text="{Binding ActualReceivedAmount, StringFormat=N2, UpdateSourceTrigger=PropertyChanged}"/>
            </Grid>
        </Border>
        
        <!-- 数据表格区域 -->
        <Border Grid.Row="4" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Padding="15" Background="White" Margin="0,10,0,0">
            <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" SelectionUnit="FullRow" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="序号" Width="60"/>
                    <DataGridTextColumn Header="配件名称" Width="*"/>
                    <DataGridTextColumn Header="规格型号" Width="*"/>
                    <DataGridTextColumn Header="数量" Width="80"/>
                    <DataGridTextColumn Header="单价" Width="100"/>
                    <DataGridTextColumn Header="金额" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        
        <!-- 空行 -->
        <Grid Grid.Row="5"/>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="结算" Command="{Binding SettleCommand}" Width="80"/>
        </StackPanel>
        
        <!-- 加载指示器 -->
        <Grid Grid.Row="0" Grid.RowSpan="7" Background="#07FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Loading" Width="60" Height="60" Foreground="#3498DB"/>
                <TextBlock Text="处理中..." FontSize="16" Margin="0,10,0,0" Foreground="#3498DB"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>