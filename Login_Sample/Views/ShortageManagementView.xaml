<UserControl x:Class="Login_Sample.Views.ShortageManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Grid Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="PackageOpen" Width="32" Height="32" Foreground="#3498DB"/>
                <TextBlock Text="缺件管理" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" Margin="10,0,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Column="1">
                <Button Content="新增缺件" Command="{Binding AddShortageCommand}" Margin="0,0,10,0"/>
                <Button Content="刷新" Command="{Binding RefreshCommand}"/>
            </StackPanel>
        </Grid>

        <!-- 查询条件区域 -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 查询条件行1 -->
                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" Margin="0,0,10,0">
                    <TextBlock Text="客户姓名" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CustomerName}" Width="200"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Vertical" Margin="0,0,10,0">
                    <TextBlock Text="车牌号码" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding LicensePlate}" Width="200"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Vertical" Margin="0,0,10,0">
                    <TextBlock Text="配件名称" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding PartName}" Width="200"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="3" Orientation="Vertical" Margin="0,0,10,0">
                    <TextBlock Text="配件编号" FontWeight="Medium" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding PartNumber}" Width="200"/>
                </StackPanel>

                <!-- 查询条件行2 -->
                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical" Margin="0,15,10,0">
                    <TextBlock Text="状态" FontWeight="Medium" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" Width="200"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="1" Orientation="Vertical" Margin="0,15,10,0">
                    <TextBlock Text="登记日期范围" FontWeight="Medium" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal">
                        <DatePicker SelectedDate="{Binding StartDate}" Width="120" Margin="0,0,10,0"/>
                        <TextBlock Text="至" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <DatePicker SelectedDate="{Binding EndDate}" Width="120"/>
                    </StackPanel>
                </StackPanel>

                <!-- 查询按钮 -->
                <StackPanel Grid.Row="1" Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,15,10,0">
                    <Button Content="查询" Command="{Binding SearchCommand}" Margin="0,0,10,0"/>
                    <Button Content="重置" Command="{Binding ResetCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 结果显示区域 -->
        <Border Grid.Row="2" Background="White" CornerRadius="5" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 缺件列表 -->
                <DataGrid ItemsSource="{Binding ShortageItems}"
                          AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow"
                          MouseDoubleClick="DataGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="序号" Width="50" IsReadOnly="True" Binding="{Binding ShortageId}"/>
                        <DataGridTextColumn Header="客户姓名" Width="120" IsReadOnly="True" Binding="{Binding CustomerName}"/>
                        <DataGridTextColumn Header="联系电话" Width="120" IsReadOnly="True" Binding="{Binding PhoneNumber}"/>
                        <DataGridTextColumn Header="车牌号码" Width="100" IsReadOnly="True" Binding="{Binding LicensePlate}"/>
                        <DataGridTextColumn Header="车辆品牌" Width="100" IsReadOnly="True" Binding="{Binding VehicleBrand}"/>
                        <DataGridTextColumn Header="车辆型号" Width="150" IsReadOnly="True" Binding="{Binding VehicleModel}"/>
                        <DataGridTextColumn Header="配件名称" Width="120" IsReadOnly="True" Binding="{Binding PartName}"/>
                        <DataGridTextColumn Header="配件编号" Width="120" IsReadOnly="True" Binding="{Binding PartNumber}"/>
                        <DataGridTextColumn Header="数量" Width="60" IsReadOnly="True" Binding="{Binding Quantity}"/>
                        <DataGridTextColumn Header="登记时间" Width="150" IsReadOnly="True" Binding="{Binding RegisteredTime, StringFormat=yyyy-MM-dd HH:mm}"/>
                        <DataGridTextColumn Header="状态" Width="80" IsReadOnly="True" Binding="{Binding Status}">
                            <DataGridTextColumn.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="已确认">
                                            <Setter Property="Foreground" Value="#3498DB"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="已订货">
                                            <Setter Property="Foreground" Value="#F39C12"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="已到货">
                                            <Setter Property="Foreground" Value="#27AE60"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="已通知">
                                            <Setter Property="Foreground" Value="#8E44AD"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="已处理">
                                            <Setter Property="Foreground" Value="#95A5A6"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.CellStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="备注" Width="*" IsReadOnly="True" Binding="{Binding Remarks}"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 分页信息 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <TextBlock Text="共 {Binding TotalItems} 条记录" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="上一页" Command="{Binding PreviousPageCommand}" IsEnabled="{Binding CanGoPrevious}"/>
                    <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <TextBlock Text="/" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" Margin="5,0,10,0"/>
                    <Button Content="下一页" Command="{Binding NextPageCommand}" IsEnabled="{Binding CanGoNext}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>

    <!-- 资源定义 -->
    <UserControl.Resources>
        <!-- 文本框样式 -->
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0,0,0"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#3498DB" BlurRadius="5" Opacity="0.3"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 日期选择器样式 -->
        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Height" Value="36"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- 按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="15,0,15,0"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1F618D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="15,0,15,0"/>
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#BDC3C7"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#95A5A6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PaginationButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5,0,5,0"/>
        </Style>

        <!-- 数据网格样式 -->
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="VerticalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ECF0F1"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3498DB"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
</UserControl>