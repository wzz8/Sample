<UserControl x:Class="Login_Sample.Views.SparePartsInboundView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Warehouse" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="备件入库" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <!-- 入库信息表单 -->
            <materialDesign:Card Margin="0,0,0,10" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="入库单号" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding InboundNumber}" IsReadOnly="True" Background="#F5F5F5"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="入库人" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding InboundPerson}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="入库日期" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding InboundDate}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,15,15">
                        <TextBlock Text="不含税总金额" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TotalAmountWithoutTax, StringFormat={}{0:C2}}" IsReadOnly="True" Background="#F5F5F5"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,0,15,15">
                        <TextBlock Text="含税总金额" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TotalAmountWithTax, StringFormat={}{0:C2}}" IsReadOnly="True" Background="#F5F5F5"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,15,15">
                        <TextBlock Text="备注" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Remarks}" Height="60" AcceptsReturn="True"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
            
            <!-- 入库备件列表 -->
            <materialDesign:Card Grid.Row="1" Margin="0,10,0,0" Padding="15">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="入库备件列表" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                        
                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                            <Button Command="{Binding AddInboundItemCommand}" Background="#2ECC71" Foreground="White" Margin="0,0,10,0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock>添加入库项</TextBlock>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                            
                            <Button Command="{Binding SaveInboundCommand}" Background="#3498DB" Foreground="White" Margin="0,0,10,0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock>保存入库</TextBlock>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                            
                            <Button Command="{Binding ReturnSparePartsCommand}" Background="#E74C3C" Foreground="White" Margin="0,0,10,0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ArrowLeft" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock>备件退货</TextBlock>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                            
                            <Button Command="{Binding ResetCommand}" Background="#95A5A6" Foreground="White">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock>重置</TextBlock>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </Grid>
                    
                    <!-- 入库备件数据表格 -->
                    <DataGrid ItemsSource="{Binding InboundItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="备件名称" Binding="{Binding InventoryItem.ItemName}" Width="150"/>
                            <DataGridTextColumn Header="备件编号" Binding="{Binding InventoryItem.ItemNumber}" Width="150"/>
                            <DataGridTextColumn Header="供应商" Binding="{Binding InventoryItem.Supplier}" Width="150"/>
                            <DataGridTextColumn Header="入库数量" Binding="{Binding Quantity}" Width="80"/>
                            <DataGridTextColumn Header="不含税单价" Binding="{Binding UnitPriceWithoutTax, StringFormat={}{0:C2}}" Width="100"/>
                            <DataGridTextColumn Header="税率" Binding="{Binding TaxRate, StringFormat={}{0:P2}}" Width="80"/>
                            <DataGridTextColumn Header="含税单价" Binding="{Binding UnitPriceWithTax, StringFormat={}{0:C2}}" Width="100"/>
                            <DataGridTextColumn Header="不含税小计" Binding="{Binding SubtotalWithoutTax, StringFormat={}{0:C2}}" Width="100"/>
                            <DataGridTextColumn Header="含税小计" Binding="{Binding SubtotalWithTax, StringFormat={}{0:C2}}" Width="100"/>
                            <DataGridTemplateColumn Header="操作" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Command="{Binding DataContext.RemoveInboundItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Background="#E74C3C" Foreground="White" Width="60">
                                            <Button.Content>
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="Delete" Width="14" Height="14" Margin="0,0,3,0"/>
                                                    <TextBlock>删除</TextBlock>
                                                </StackPanel>
                                            </Button.Content>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>