<UserControl x:Class="Login_Sample.Views.AppointmentManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Login_Sample.Views"
        xmlns:viewModels="clr-namespace:Login_Sample.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        d:DesignHeight="700" d:DesignWidth="900">
    <UserControl.DataContext>
        <viewModels:AppointmentManagementViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="CalendarClock" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="预约管理" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 内容区域 -->
        <Grid Grid.Row="1" Margin="10 10 10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- 左侧预约表单 -->
            <Border BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="预约信息" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />
                    
                    <!-- 客户选择 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="客户" Margin="0,0,0,5" />
                        <ComboBox ItemsSource="{Binding Customers}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCustomer}" Width="350" />
                    </StackPanel>
                    
                    <!-- 车辆选择 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="车辆" Margin="0,0,0,5" />
                        <ComboBox ItemsSource="{Binding CustomerVehicles}" DisplayMemberPath="LicensePlate" SelectedItem="{Binding SelectedVehicle}" Width="350" />
                    </StackPanel>
                    
                    <!-- 维修技师选择 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="维修技师（可选）" Margin="0,0,0,5" />
                        <ComboBox ItemsSource="{Binding Technicians}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedTechnician}" Width="350" />
                    </StackPanel>
                    
                    <!-- 预约类型 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="预约类型" Margin="0,0,0,5" />
                        <ComboBox ItemsSource="{Binding AppointmentTypeOptions}" SelectedItem="{Binding AppointmentType}" Width="350" />
                    </StackPanel>
                    
                    <!-- 预约日期 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="预约日期" Margin="0,0,0,5" />
                        <DatePicker SelectedDate="{Binding AppointmentDate}" Width="350" />
                    </StackPanel>
                    
                    <!-- 预约时间 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="预约时间" Margin="0,0,0,5" />
                        <ComboBox ItemsSource="{Binding TimeOptions}" SelectedItem="{Binding AppointmentTime}" Width="350" />
                    </StackPanel>
                    
                    <!-- 服务内容 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="服务内容" Margin="0,0,0,5" />
                        <TextBox Text="{Binding ServiceContent}" Width="350" Height="60" TextWrapping="Wrap" />
                    </StackPanel>
                    
                    <!-- 备注 -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="备注" Margin="0,0,0,5" />
                        <TextBox Text="{Binding Remarks}" Width="350" Height="40" TextWrapping="Wrap" />
                    </StackPanel>
                    
                    <!-- 按钮 -->
                    <Button Content="创建预约" Command="{Binding AddAppointmentCommand}" Width="150" Height="35" Background="#FF007ACC" Foreground="White" FontWeight="Bold" />
                </StackPanel>
            </Border>
            
            <!-- 右侧预约列表 -->
            <Border Grid.Column="1" BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="5" Padding="10" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="预约列表" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" />
                    
                    <!-- 预约数据表格 -->
                    <DataGrid ItemsSource="{Binding Appointments}" AutoGenerateColumns="False" Height="Auto" CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="预约ID" Binding="{Binding Id}" Width="80" />
                            <DataGridTextColumn Header="客户" Binding="{Binding Customer.Name}" Width="120" />
                            <DataGridTextColumn Header="车辆" Binding="{Binding Vehicle.LicensePlate}" Width="100" />
                            <DataGridTextColumn Header="技师" Binding="{Binding Technician.Name}" Width="100" />
                            <DataGridTextColumn Header="类型" Binding="{Binding AppointmentType}" Width="100" />
                            <DataGridTextColumn Header="日期" Binding="{Binding AppointmentDate, StringFormat=yyyy-MM-dd}" Width="100" />
                            <DataGridTextColumn Header="时间" Binding="{Binding AppointmentTime}" Width="100" />
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100" />
                            <DataGridTemplateColumn Header="操作" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="编辑" Margin="0,0,5,0" Width="50" Height="25" Background="#FFE5E5E5" />
                                            <Button Content="取消" Width="50" Height="25" Background="#FFFF6B6B" Foreground="White" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>