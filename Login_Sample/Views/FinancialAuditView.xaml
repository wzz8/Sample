<UserControl x:Class="Login_Sample.Views.FinancialAuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:viewModels="clr-namespace:Login_Sample.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:FinancialAuditViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Grid Grid.Row="0" Background="#2C3E50" Height="60">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="CheckCircle" Width="28" Height="28" Foreground="White"/>
                <TextBlock Text="财务审核" FontSize="20" FontWeight="Bold" Foreground="White" Margin="15,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- 主要内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 搜索条件区域 -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="搜索关键字:" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SearchKey, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="单据类型:" Margin="0,0,0,5"/>
                    <ComboBox Height="30">
                        <ComboBoxItem Content="全部"/>
                        <ComboBoxItem Content="维修单"/>
                        <ComboBoxItem Content="配件销售单"/>
                        <ComboBoxItem Content="采购入库单"/>
                        <ComboBoxItem Content="销售结算单"/>
                        <ComboBoxItem Content="款项单"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="审核状态:" Margin="0,0,0,5"/>
                    <ComboBox Height="30">
                        <ComboBoxItem Content="全部"/>
                        <ComboBoxItem Content="未审核"/>
                        <ComboBoxItem Content="已审核"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Command="{Binding SearchDocumentsCommand}" Margin="0,0,10,0" Width="80" Height="30">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Magnify" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="搜索"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding RefreshDocumentsCommand}" Width="80" Height="30">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="刷新"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 错误提示 -->
            <Grid Grid.Row="1" Background="#FEEFB3" Visibility="{Binding ErrorVisibility, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,0,10" Height="40">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <materialDesign:PackIcon Kind="Alert" Width="20" Height="20" Foreground="#9F6000"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#9F6000" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- 单据列表 -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <!-- 数据表格 -->
                <DataGrid Grid.Row="0" ItemsSource="{Binding AuditDocuments}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                          SelectedItem="{Binding SelectedDocument}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="单据ID" Binding="{Binding DocumentId}" Width="150"/>
                        <DataGridTextColumn Header="单据类型" Binding="{Binding DocumentType}" Width="120"/>
                        <DataGridTextColumn Header="单据日期" Binding="{Binding DocumentDate}" Width="120"/>
                        <DataGridTextColumn Header="客户名称" Binding="{Binding CustomerName}" Width="120"/>
                        <DataGridTextColumn Header="总金额" Binding="{Binding TotalAmount}" Width="100"/>
                        <DataGridTextColumn Header="审核状态" Binding="{Binding AuditStatus}" Width="100"/>
                        <DataGridTextColumn Header="审核日期" Binding="{Binding AuditDate}" Width="150"/>
                        <DataGridTextColumn Header="审核人" Binding="{Binding Auditor}" Width="120"/>
                        <DataGridTextColumn Header="备注" Binding="{Binding Remarks}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 操作按钮 -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Command="{Binding AuditDocumentCommand}" Margin="0,0,10,0" Width="100" Height="40">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Check" Width="20" Height="20" Margin="0,0,5,0"/>
                            <TextBlock Text="审核"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding CancelAuditCommand}" Width="100" Height="40">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Cancel" Width="20" Height="20" Margin="0,0,5,0"/>
                            <TextBlock Text="撤销审核"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- 加载指示器 -->
        <Grid Grid.Row="0" Grid.RowSpan="2" Background="#BBFFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Loading" Width="60" Height="60" Foreground="#3498DB"/>
                <TextBlock Text="处理中..." FontSize="16" Margin="0,10,0,0" Foreground="#3498DB"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>