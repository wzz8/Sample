<UserControl x:Class="Login_Sample.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:vm="clr-namespace:Login_Sample.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <UserControl.Resources>
        <!-- 仪表盘卡片样式 -->
        <Style x:Key="DashboardCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 模块标题样式 -->
        <Style x:Key="ModuleTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="10,10,0,0"/>
        </Style>

        <!-- 数据值样式 -->
        <Style x:Key="DataValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#3498DB"/>
            <Setter Property="Margin" Value="10,5,0,0"/>
        </Style>

        <!-- 数据标签样式 -->
        <Style x:Key="DataLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin" Value="10,5,0,0"/>
        </Style>

        <!-- 进度条样式 -->
        <Style x:Key="DashboardProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Margin" Value="10,5,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Rectangle RadiusX="4" RadiusY="4" Fill="{TemplateBinding Background}"/>
                            <Rectangle RadiusX="4" RadiusY="4" Fill="#3498DB" HorizontalAlignment="Left"
                                   Width="{TemplateBinding Value}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel Background="#F5F5F5" Orientation="Vertical" VerticalAlignment="Top">
        <!-- 欢迎标题 -->
        <Border Background="#FFFFFF" Height="80" CornerRadius="5">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="ViewDashboard" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <StackPanel>
                    <TextBlock Text="欢迎使用企业管理系统" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="实时监控各模块运营数据" FontSize="14" Foreground="#7F8C8D"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 数据卡片区域 -->
        <ScrollViewer Margin="0,0,0,0" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <!-- 第一行：业务接待、财务结算、备件管理 -->
                <Grid Grid.Row="0" Height="200">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 业务接待模块卡片 -->
                    <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="业务接待" Style="{StaticResource ModuleTitleStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                                <materialDesign:PackIcon Kind="AccountGroup" Width="24" Height="24" Foreground="#3498DB" Margin="0,10,10,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding TodayReceptions}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="今日接待量" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="ClockOutline" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="待处理业务: "/>
                                <Run Text="{Binding PendingBusinesses}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="CheckCircleOutline" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="完成率: "/>
                                <Run Text="{Binding BusinessCompletionRate, StringFormat={}{0:F1}%}"/>
                                </TextBlock>
                            </StackPanel>
                            <ProgressBar Value="{Binding BusinessCompletionRate}" Maximum="100" Style="{StaticResource DashboardProgressBarStyle}" Margin="10,10"/>
                        </StackPanel>
                    </Border>

                    <!-- 财务结算模块卡片 -->
                    <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="财务结算" Style="{StaticResource ModuleTitleStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                                <materialDesign:PackIcon Kind="Cash" Width="24" Height="24" Foreground="#3498DB" Margin="0,10,10,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding TodayRevenue, StringFormat={}{0:C}}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="今日收入" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="ClockOutline" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="待结算金额: "/>
                                <Run Text="{Binding PendingSettlement, StringFormat={}{0:C}}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="Calendar" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="本月收入: "/>
                                <Run Text="{Binding MonthRevenue, StringFormat={}{0:C}}"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 备件管理模块卡片 -->
                    <Border Grid.Column="2" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="备件管理" Style="{StaticResource ModuleTitleStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                                <materialDesign:PackIcon Kind="Box" Width="24" Height="24" Foreground="#3498DB" Margin="0,10,10,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding TotalSpareParts}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="备件总数" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="AlertCircleOutline" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="低库存预警: "/>
                                <Run Text="{Binding LowStockAlerts}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="Money" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="今日消耗: "/>
                                <Run Text="{Binding TodayConsumptions}"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 第二行：车间管理、领导查询、系统管理 -->
                <Grid Grid.Row="1" Height="200">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 车间管理模块卡片 -->
                    <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="车间管理" Style="{StaticResource ModuleTitleStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                                <materialDesign:PackIcon Kind="Factory" Width="24" Height="24" Foreground="#3498DB" Margin="0,10,10,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding RepairingVehicles}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="维修中车辆" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="CheckCircleOutline" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="完成率: "/>
                                <Run Text="{Binding WorkshopCompletionRate, StringFormat={}{0:F1}%}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="Car2Plus" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="今日完工: "/>
                                <Run Text="{Binding TodayCompletedRepairs}"/>
                                </TextBlock>
                            </StackPanel>
                            <ProgressBar Value="{Binding WorkshopCompletionRate}" Maximum="100" Style="{StaticResource DashboardProgressBarStyle}" Margin="10,10"/>
                        </StackPanel>
                    </Border>

                    <!-- 领导查询模块卡片 -->
                    <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="领导查询" Style="{StaticResource ModuleTitleStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                                <materialDesign:PackIcon Kind="ChartLine" Width="24" Height="24" Foreground="#3498DB" Margin="0,10,10,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding OverallEfficiency, StringFormat={}{0:F1}%}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="整体效率" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="TrendingUp" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="利润率: "/>
                                <Run Text="{Binding ProfitMargin, StringFormat={}{0:F1}%}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="StarOutline" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="客户满意度: "/>
                                <Run Text="{Binding CustomerSatisfaction, StringFormat={}{0}%}"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 系统管理模块卡片 -->
                    <Border Grid.Column="2" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="系统管理" Style="{StaticResource ModuleTitleStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                                <materialDesign:PackIcon Kind="Account" Width="24" Height="24" Foreground="#3498DB" Margin="0,10,10,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding OnlineUsers}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="在线用户" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="AlertCircleOutline" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="系统警报: "/>
                                <Run Text="{Binding SystemAlerts}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="10,5,0,0">
                                <materialDesign:PackIcon Kind="CheckCircle" Width="16" Height="16" Foreground="#7F8C8D" Margin="36,5,5,0"/>
                                <TextBlock Style="{StaticResource DataLabelStyle}">
                                <Run Text="系统状态: "/>
                                <Run Text="{Binding SystemStatus}"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 第三行：原始库存 -->
                <Grid Grid.Row="2" Height="200">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 原始库存模块卡片 -->
                    <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <TextBlock Text="原始库存" Style="{StaticResource ModuleTitleStyle}"/>
                            <StackPanel Orientation="Horizontal" Margin="10,10,0,0">
                                <materialDesign:PackIcon Kind="Warehouse" Width="24" Height="24" Foreground="#3498DB" Margin="0,10,15,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding TotalInventoryValue, StringFormat={}{0:C}}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="库存总值" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                                <StackPanel Margin="50,0,0,0">
                                    <TextBlock Text="{Binding InventoryTurnover, StringFormat={}{0:F1}}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="周转率" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                                <StackPanel Margin="50,0,0,0">
                                    <TextBlock Text="{Binding TotalInventoryItems}" Style="{StaticResource DataValueStyle}"/>
                                    <TextBlock Text="库存总项数" Style="{StaticResource DataLabelStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>
    </StackPanel>
</UserControl>
