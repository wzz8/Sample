<UserControl x:Class="Login_Sample.Views.PaymentProcessingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:viewModels="clr-namespace:Login_Sample.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:PaymentProcessingViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Grid Grid.Row="0" Background="#2C3E50" Height="60">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Bank" Width="28" Height="28" Foreground="White"/>
                <TextBlock Text="款项处理" FontSize="20" FontWeight="Bold" Foreground="White" Margin="15,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- 主要内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 操作按钮区域 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <Button Command="{Binding IncomeCommand}" Margin="0,0,10,0" Width="100" Height="40">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="ArrowDownCircle" Width="20" Height="20" Margin="0,0,5,0"/>
                        <TextBlock Text="收入"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ExpenseCommand}" Margin="0,0,10,0" Width="100" Height="40">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="ArrowUpCircle" Width="20" Height="20" Margin="0,0,5,0"/>
                        <TextBlock Text="支出"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding CorrectPaymentCommand}" Margin="0,0,10,0" Width="120" Height="40">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Edit" Width="20" Height="20" Margin="0,0,5,0"/>
                        <TextBlock Text="收款改错"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding RefreshRecordsCommand}" Margin="0,0,10,0" Width="100" Height="40">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20" Margin="0,0,5,0"/>
                        <TextBlock Text="刷新"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- 错误提示 -->
            <Grid Grid.Row="1" Background="#FEEFB3" Visibility="{Binding ErrorVisibility, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,0,10" Height="40">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <materialDesign:PackIcon Kind="Alert" Width="20" Height="20" Foreground="#9F6000"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#9F6000" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- 款项记录数据表格 -->
            <DataGrid Grid.Row="2" ItemsSource="{Binding PaymentRecords}" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      SelectedItem="{Binding SelectedPaymentRecord}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="款项单位" Binding="{Binding PaymentUnit}" Width="150"/>
                    <DataGridTextColumn Header="款项类型" Binding="{Binding PaymentType}" Width="100"/>
                    <DataGridTextColumn Header="金额" Binding="{Binding Amount}" Width="100" />
                    <DataGridTextColumn Header="款项日期" Binding="{Binding PaymentDate}" Width="120"/>
                    <DataGridTextColumn Header="备注" Binding="{Binding Remarks}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- 加载指示器 -->
        <Grid Grid.Row="0" Grid.RowSpan="2" Background="#07FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Loading" Width="60" Height="60" Foreground="#3498DB"/>
                <TextBlock Text="处理中..." FontSize="16" Margin="0,10,0,0" Foreground="#3498DB"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>