<UserControl x:Class="Login_Sample.Views.SparePartsManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Box" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="备件管理" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧：备件表单 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="新增备件" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <TextBlock Text="备件信息" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <TextBox materialDesign:HintAssist.Hint="备件名称" Margin="0,0,0,10"/>
                            <TextBox materialDesign:HintAssist.Hint="备件编号" Margin="0,0,0,10"/>
                            <TextBox materialDesign:HintAssist.Hint="库存数量" Margin="0,0,0,10"/>
                            <TextBox materialDesign:HintAssist.Hint="单价" Margin="0,0,0,10"/>
                            <TextBox materialDesign:HintAssist.Hint="供应商" Margin="0,0,0,15"/>
                            
                            <Button Content="保存备件" Background="#3498DB" Foreground="White" Margin="0,10,0,0"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Border>
            
            <!-- 右侧：备件列表 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="备件库存" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>
                    
                    <DataGrid AutoGenerateColumns="False" Margin="10" CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="备件编号" Width="120"/>
                            <DataGridTextColumn Header="备件名称" Width="150"/>
                            <DataGridTextColumn Header="库存数量" Width="100"/>
                            <DataGridTextColumn Header="单价" Width="100"/>
                            <DataGridTextColumn Header="供应商" Width="150"/>
                            <DataGridTextColumn Header="库存状态" Width="100"/>
                            <DataGridTemplateColumn Header="操作" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="编辑" Margin="2" />
                                            <Button Content="出库" Margin="2" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <!-- 移除直接的数据定义，稍后通过ViewModel绑定 -->
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>