<UserControl x:Class="Login_Sample.Views.SparePartsBorrowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:Login_Sample.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- 转换器 -->
        <converters:StringToBooleanConverter x:Key="StringToBooleanConverter" />
        
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="Margin" Value="0,5,10,5"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,5,10,5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Foreground" Value="#AAAAAA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="BorderBrush" Value="#2980B9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1F618D"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDC3C7"/>
                    <Setter Property="BorderBrush" Value="#95A5A6"/>
                    <Setter Property="Cursor" Value="No"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,5,10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="35"/>
        </Style>

        <Style x:Key="DataGridColumnStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#ECF0F1">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 页面标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="BookArrowUp" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="备件借用" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 借用记录信息 -->
            <GroupBox Header="借用记录信息" Grid.Row="0" Margin="20,0,20,20" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="借用记录ID:" Style="{StaticResource LabelStyle}" Grid.Column="0" Grid.Row="0"/>
                    <TextBox Text="{Binding BorrowRecordId}" Style="{StaticResource TextBoxStyle}" Grid.Column="1" Grid.Row="0" IsEnabled="False"/>

                    <Label Content="借用日期:" Style="{StaticResource LabelStyle}" Grid.Column="2" Grid.Row="0"/>
                    <DatePicker SelectedDate="{Binding BorrowDate}" Style="{StaticResource DatePickerStyle}" Grid.Column="3" Grid.Row="0" IsEnabled="False"/>

                    <Label Content="借用人:" Style="{StaticResource LabelStyle}" Grid.Column="4" Grid.Row="0"/>
                    <TextBox Text="{Binding Borrower}" Style="{StaticResource TextBoxStyle}" Grid.Column="5" Grid.Row="0"/>

                    <Label Content="归还日期:" Style="{StaticResource LabelStyle}" Grid.Column="0" Grid.Row="1"/>
                    <DatePicker SelectedDate="{Binding ReturnDate}" Style="{StaticResource DatePickerStyle}" Grid.Column="1" Grid.Row="1" IsEnabled="{Binding Path=BorrowStatus, Converter={StaticResource StringToBooleanConverter}}"/>

                    <Label Content="借用状态:" Style="{StaticResource LabelStyle}" Grid.Column="2" Grid.Row="1"/>
                    <TextBox Text="{Binding BorrowStatus}" Style="{StaticResource TextBoxStyle}" Grid.Column="3" Grid.Row="1" IsEnabled="False"/>

                    <Label Content="借用备注:" Style="{StaticResource LabelStyle}" Grid.Column="0" Grid.Row="2"/>
                    <TextBox Text="{Binding BorrowRemarks}" Style="{StaticResource TextBoxStyle}" Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="3" AcceptsReturn="True" Height="70" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>

                    <StackPanel Orientation="Horizontal" Grid.Column="4" Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Bottom">
                        <Button Content="开始借用" Command="{Binding BorrowSparePartsCommand}" Style="{StaticResource ButtonStyle}" Background="#27AE60" BorderBrush="#229954"/>
                        <Button Content="归还备件" Command="{Binding ReturnSparePartsCommand}" Style="{StaticResource ButtonStyle}" Background="#E74C3C" BorderBrush="#C0392B"/>
                        <Button Content="刷新记录" Command="{Binding RefreshBorrowRecordsCommand}" Style="{StaticResource ButtonStyle}" Background="#F39C12" BorderBrush="#E67E22"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- 借用项列表 -->
            <GroupBox Header="借用项列表" Grid.Row="1" Margin="20,0,20,20" Padding="10">
                <DataGrid ItemsSource="{Binding BorrowItems}" Style="{StaticResource DataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="配件ID" Binding="{Binding InventoryItemId}" Width="80" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="配件名称" Binding="{Binding ItemName}" Width="150" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="配件编号" Binding="{Binding ItemNumber}" Width="120" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="借用数量" Binding="{Binding BorrowQuantity}" Width="100" HeaderStyle="{StaticResource DataGridColumnStyle}" IsReadOnly="False"/>
                        <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice, StringFormat=C2}" Width="100" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="供应商" Binding="{Binding Supplier}" Width="150" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="仓库位置" Binding="{Binding WarehouseLocation}" Width="150" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- 借用记录列表 -->
            <GroupBox Header="借用记录历史" Grid.Row="2" Margin="20,0,20,20" Padding="10">
                <DataGrid ItemsSource="{Binding BorrowRecords}" Style="{StaticResource DataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="记录ID" Binding="{Binding BorrowRecordId}" Width="120" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="借用日期" Binding="{Binding BorrowDate, StringFormat=yyyy-MM-dd HH:mm}" Width="150" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="借用人" Binding="{Binding Borrower}" Width="100" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="归还日期" Binding="{Binding ReturnDate, StringFormat=yyyy-MM-dd HH:mm}" Width="150" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                        <DataGridTextColumn Header="借用状态" Binding="{Binding BorrowStatus}" Width="100" HeaderStyle="{StaticResource DataGridColumnStyle}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BorrowStatus}" Value="借用中">
                                            <Setter Property="Foreground" Value="#E74C3C"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding BorrowStatus}" Value="已归还">
                                            <Setter Property="Foreground" Value="#27AE60"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="备注" Binding="{Binding BorrowRemarks}" Width="*" HeaderStyle="{StaticResource DataGridColumnStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>