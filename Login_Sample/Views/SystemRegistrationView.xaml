<UserControl x:Class="Login_Sample.Views.SystemRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="FileCode" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="系统注册" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>

        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：注册操作 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="注册操作" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>

                    <materialDesign:Card Margin="10" Padding="10">
                        <StackPanel>
                            <Button Content="生成注册文件" Background="#3498DB" Foreground="White" Margin="0,5,0,5"/>
                            <Button Content="导入注册文件" Background="#3498DB" Foreground="White" Margin="0,5,0,5"/>
                            <Button Content="验证注册状态" Background="#3498DB" Foreground="White" Margin="0,5,0,5"/>
                            <Button Content="查看机器码" Background="#3498DB" Foreground="White" Margin="0,5,0,5"/>
                        </StackPanel>
                    </materialDesign:Card>

                    <TextBlock Text="注册状态" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="15,20,0,10"/>
                    <ListBox Margin="10">
                        <ListBoxItem>未注册</ListBoxItem>
                        <ListBoxItem>已注册</ListBoxItem>
                        <ListBoxItem>注册过期</ListBoxItem>
                        <ListBoxItem>注册失败</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- 右侧：机器码和注册信息 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Grid.Column="1">
                <ScrollViewer>
                    <StackPanel>
                        <TextBlock Text="机器码和注册信息" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="15,15,0,15"/>

                        <!-- 机器码信息 -->
                        <materialDesign:Card Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="机器码" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="ABCDE-FGHIJ-KLMNO-PQRST-UVWXY" FontSize="14" Margin="0,0,0,10" Background="#F8F9FA" Padding="10" TextWrapping="Wrap"/>
                                <TextBlock Text="机器码说明：" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                                <TextBlock Text="1. 机器码是根据您的计算机硬件生成的唯一标识" FontSize="14" Margin="10,0,0,5"/>
                                <TextBlock Text="2. 请将此机器码提供给系统管理员" FontSize="14" Margin="10,0,0,5"/>
                                <TextBlock Text="3. 系统管理员将根据此机器码生成注册文件" FontSize="14" Margin="10,0,0,5"/>
                                <Button Content="复制机器码" Background="#3498DB" Foreground="White" Margin="0,15,0,5"/>
                            </StackPanel>
                        </materialDesign:Card>

                        <!-- 注册文件信息 -->
                        <materialDesign:Card Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="注册文件" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="注册文件格式：.reg" FontSize="14" Margin="0,0,0,10"/>
                                <TextBlock Text="注册文件路径：" FontSize="14" Margin="0,0,0,5"/>
                                <TextBlock Text="C:\\Registration\\System.reg" FontSize="14" Margin="0,0,0,10" Background="#F8F9FA" Padding="10"/>
                                <TextBlock Text="注册说明：" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                                <TextBlock Text='1. 点击"导入注册文件"按钮选择生成的注册文件' FontSize="14" Margin="10,0,0,5"/>
                                <TextBlock Text="2. 系统将自动验证注册文件的有效性" FontSize="14" Margin="10,0,0,5"/>
                                <TextBlock Text="3. 验证通过后，系统将完成注册" FontSize="14" Margin="10,0,0,5"/>
                                <Button Content="浏览注册文件" Background="#3498DB" Foreground="White" Margin="0,15,0,5"/>
                            </StackPanel>
                        </materialDesign:Card>

                        <!-- 文本粘贴和网络获取注册信息 -->
                        <materialDesign:Card Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="其他注册方式" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                                <!-- 文本粘贴注册 -->
                                <TextBlock Text="文本粘贴注册" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                                <TextBox Text="" materialDesign:HintAssist.Hint="请粘贴注册文本" Margin="0,0,0,10" Height="100" TextWrapping="Wrap"/>
                                <Button Content="粘贴注册文本" Background="#3498DB" Foreground="White" Margin="0,0,10,10" Width="120"/>

                                <!-- 网络获取注册 -->
                                <TextBlock Text="网络获取注册" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Text="" materialDesign:HintAssist.Hint="注册码" Margin="0,0,10,10" Width="200"/>
                                    <Button Content="通过网络获取" Background="#3498DB" Foreground="White" Margin="0,0,0,10"/>
                                </StackPanel>
                                <TextBlock Text="注册说明：" FontSize="14" FontWeight="Bold" Margin="0,10,0,5"/>
                                <TextBlock Text="1. 文本粘贴：将获取的注册文本粘贴到文本框中，点击粘贴注册文本按钮" FontSize="14" Margin="10,0,0,5"/>
                                <TextBlock Text="2. 网络获取：输入注册码，点击通过网络获取按钮，系统将自动获取注册信息" FontSize="14" Margin="10,0,0,5"/>
                                <TextBlock Text="3. 验证通过后，系统将完成注册" FontSize="14" Margin="10,0,0,5"/>
                            </StackPanel>
                        </materialDesign:Card>

                        <!-- 注册状态信息 -->
                        <materialDesign:Card Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="注册状态信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="注册状态：未注册" FontSize="14" Margin="0,0,0,5"/>
                                <TextBlock Text="注册有效期：无" FontSize="14" Margin="0,0,0,5"/>
                                <TextBlock Text="注册日期：无" FontSize="14" Margin="0,0,0,5"/>
                                <TextBlock Text="过期日期：无" FontSize="14" Margin="0,0,0,5"/>
                                <TextBlock Text="注册错误信息：无" FontSize="14" Margin="0,0,0,5" Foreground="#E74C3C"/>
                            </StackPanel>
                        </materialDesign:Card>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>