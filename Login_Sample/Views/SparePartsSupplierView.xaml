<UserControl x:Class="Login_Sample.Views.SparePartsSupplierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="AccountGroup" Width="32" Height="32" Foreground="#2980B9" Margin="0,0,15,0"/>
                <TextBlock Text="备件供货商" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 供应商信息表单 -->
            <materialDesign:Card Margin="0,0,0,10" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="供应商ID" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SupplierId}" IsReadOnly="True" Background="#F5F5F5"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="供应商名称" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SupplierName}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="联系人" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ContactPerson}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="3" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="联系电话" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ContactPhone}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,15,15">
                        <TextBlock Text="电子邮箱" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Email}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,0,15,15">
                        <TextBlock Text="地址" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Address}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,15,15">
                        <TextBlock Text="备注" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding Remarks}" Height="60" AcceptsReturn="True"/>
                    </StackPanel>
                    
                    <!-- 操作按钮 -->
                    <StackPanel Grid.Column="3" Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                        <Button Command="{Binding NewCommand}" Background="#27AE60" Foreground="White" Margin="0,0,10,0">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock>新增</TextBlock>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                        
                        <Button Command="{Binding SaveCommand}" Background="#3498DB" Foreground="White" Margin="0,0,10,0">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock>保存</TextBlock>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                        
                        <Button Command="{Binding EditCommand}" Background="#F39C12" Foreground="White" Margin="0,0,10,0">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock>修改</TextBlock>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                        
                        <Button Command="{Binding DeleteCommand}" Background="#E74C3C" Foreground="White">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock>删除</TextBlock>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
            
            <!-- 供应商列表 -->
            <materialDesign:Card Grid.Row="1" Margin="0,10,0,0" Padding="15">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="供应商列表" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                        
                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                            <TextBox Text="{Binding SearchText}" Width="200" Margin="0,0,10,0">
                                <materialDesign:HintAssist.Hint>搜索供应商</materialDesign:HintAssist.Hint>
                            </TextBox>
                            <Button Command="{Binding SearchCommand}" Background="#95A5A6" Foreground="White">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Magnify" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock>搜索</TextBlock>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </Grid>
                    
                    <!-- 供应商数据表格 -->
                    <DataGrid ItemsSource="{Binding Suppliers}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0" SelectionChanged="DataGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="供应商ID" Binding="{Binding SupplierId}" Width="100"/>
                            <DataGridTextColumn Header="供应商名称" Binding="{Binding SupplierName}" Width="150"/>
                            <DataGridTextColumn Header="联系人" Binding="{Binding ContactPerson}" Width="100"/>
                            <DataGridTextColumn Header="联系电话" Binding="{Binding ContactPhone}" Width="120"/>
                            <DataGridTextColumn Header="电子邮箱" Binding="{Binding Email}" Width="150"/>
                            <DataGridTextColumn Header="地址" Binding="{Binding Address}" Width="*"/>
                            <DataGridTextColumn Header="备注" Binding="{Binding Remarks}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>