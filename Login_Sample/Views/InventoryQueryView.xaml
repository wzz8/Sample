<UserControl x:Class="Login_Sample.Views.InventoryQueryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="DatabaseSearch" Width="32" Height="32" Foreground="#3498DB" Margin="0,0,15,0"/>
                <TextBlock Text="库存查询" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1">
            <!-- 查询条件区域 -->
            <Border Background="#FFFFFF" Margin="10" CornerRadius="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="查询条件" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,10,0,20"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 第一行查询条件 -->
                        <materialDesign:Card Grid.Column="0" Grid.Row="0" Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="配件名称" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBox materialDesign:HintAssist.Hint="输入配件名称" Text="{Binding ItemName, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </materialDesign:Card>
                        
                        <materialDesign:Card Grid.Column="1" Grid.Row="0" Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="配件编号" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBox materialDesign:HintAssist.Hint="输入配件编号" Text="{Binding ItemNumber, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </materialDesign:Card>
                        
                        <materialDesign:Card Grid.Column="2" Grid.Row="0" Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="网点名称" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ComboBox ItemsSource="{Binding BranchOptions}" SelectedItem="{Binding SelectedBranch}" materialDesign:HintAssist.Hint="选择网点"/>
                            </StackPanel>
                        </materialDesign:Card>
                        
                        <materialDesign:Card Grid.Column="3" Grid.Row="0" Margin="10" Padding="10">
                            <StackPanel>
                                <TextBlock Text="库存状态" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" materialDesign:HintAssist.Hint="选择状态"/>
                            </StackPanel>
                        </materialDesign:Card>
                        
                        <!-- 第二行按钮 -->
                        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="5" Grid.Row="1" Margin="10,20,10,0" HorizontalAlignment="Left">
                            <Button Content="查询" Command="{Binding SearchCommand}" Margin="0,0,10,0" Width="100"/>
                            <Button Content="重置" Command="{Binding ResetCommand}" Margin="0,0,10,0" Width="100"/>
                            <Button Content="导出" Command="{Binding ExportCommand}" Width="100"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
            
            <!-- 结果显示区域 -->
            <Border Background="#FFFFFF" Margin="10,150,10,10" CornerRadius="5" Padding="20">
                <StackPanel>
                    <TextBlock Text="库存查询结果" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,10,0,20"/>
                    
                    <DataGrid ItemsSource="{Binding InventoryItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="配件编号" Binding="{Binding ItemNumber}" Width="120"/>
                            <DataGridTextColumn Header="配件名称" Binding="{Binding ItemName}" Width="150"/>
                            <DataGridTextColumn Header="网点" Binding="{Binding BranchName}" Width="100"/>
                            <DataGridTextColumn Header="库存数量" Binding="{Binding Quantity}" Width="100"/>
                            <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice, StringFormat='¥{0:F2}'}" Width="100"/>
                            <DataGridTextColumn Header="供应商" Binding="{Binding Supplier}" Width="150"/>
                            <DataGridTextColumn Header="仓库位置" Binding="{Binding WarehouseLocation}" Width="150"/>
                            <DataGridTextColumn Header="库存状态" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="最后更新" Binding="{Binding LastUpdatedTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="180"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- 分页信息 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,10,0,0">
                        <TextBlock Text="共" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalItems}" Margin="5,0,5,0"/>
                        <TextBlock Text="条记录，当前第" FontWeight="Bold"/>
                        <TextBlock Text="{Binding CurrentPage}" Margin="5,0,5,0"/>
                        <TextBlock Text="页，共" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalPages}" Margin="5,0,5,0"/>
                        <TextBlock Text="页" FontWeight="Bold"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                            <Button Content="上一页" Command="{Binding PreviousPageCommand}" Margin="0,0,10,0"/>
                            <Button Content="下一页" Command="{Binding NextPageCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
