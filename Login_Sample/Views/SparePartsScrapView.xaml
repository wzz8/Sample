<UserControl x:Class="Login_Sample.Views.SparePartsScrapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 模块标题 -->
        <Border Background="#FFFFFF" Grid.Row="0" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="Recycle" Width="32" Height="32" Foreground="#E74C3C" Margin="0,0,15,0"/>
                <TextBlock Text="备件报废" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>
        
        <!-- 模块内容区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 报废信息表单 -->
            <materialDesign:Card Margin="0,0,0,10" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="报废记录ID" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ScrapRecordId}" IsReadOnly="True" Background="#F5F5F5"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="报废日期" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding ScrapDate}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,15,15">
                        <TextBlock Text="报废人员" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ScrapPerson}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="3" Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="报废状态" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ScrapStatus}" IsReadOnly="True" Background="#F5F5F5"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,15,0">
                        <TextBlock Text="报废原因" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ScrapReason}"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="2">
                        <TextBlock Text="备注" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding ScrapRemarks}" Height="60" AcceptsReturn="True"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
            
            <!-- 报废备件列表 -->
            <materialDesign:Card Grid.Row="1" Margin="0,10,0,10" Padding="15">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="报废备件列表" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                        
                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                            <Button Command="{Binding StartScrapCommand}" Background="#3498DB" Foreground="White" Margin="0,0,10,0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Play" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock>开始报废</TextBlock>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                            
                            <Button Command="{Binding ConfirmScrapCommand}" Background="#E74C3C" Foreground="White">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Check" Width="16" Height="16" Margin="0,0,5,0"/>
                                        <TextBlock>确认报废</TextBlock>
                                    </StackPanel>
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </Grid>
                    
                    <!-- 报废备件数据表格 -->
                    <DataGrid ItemsSource="{Binding ScrapItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="配件名称" Binding="{Binding ItemName}" Width="150"/>
                            <DataGridTextColumn Header="配件编号" Binding="{Binding ItemNumber}" Width="150"/>
                            <DataGridTextColumn Header="当前库存" Binding="{Binding CurrentStock}" Width="100"/>
                            <DataGridTextColumn Header="报废数量" Binding="{Binding ScrapQuantity}" Width="100"/>
                            <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice, StringFormat={}{0:C2}}" Width="100"/>
                            <DataGridTextColumn Header="供应商" Binding="{Binding Supplier}" Width="150"/>
                            <DataGridTextColumn Header="仓库位置" Binding="{Binding WarehouseLocation}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
            
            <!-- 报废记录历史 -->
            <materialDesign:Card Grid.Row="2" Margin="0,10,0,0" Padding="15">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="报废记录历史" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>
                        
                        <Button Command="{Binding RefreshScrapRecordsCommand}" Background="#95A5A6" Foreground="White" Grid.Column="1">
                            <Button.Content>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock>刷新记录</TextBlock>
                                </StackPanel>
                            </Button.Content>
                        </Button>
                    </Grid>
                    
                    <!-- 报废记录数据表格 -->
                    <DataGrid ItemsSource="{Binding ScrapRecords}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="报废记录ID" Binding="{Binding ScrapRecordId}" Width="180"/>
                            <DataGridTextColumn Header="报废日期" Binding="{Binding ScrapDate, StringFormat={}{0:yyyy-MM-dd}}" Width="120"/>
                            <DataGridTextColumn Header="报废人员" Binding="{Binding ScrapPerson}" Width="100"/>
                            <DataGridTextColumn Header="报废原因" Binding="{Binding ScrapReason}" Width="150"/>
                            <DataGridTextColumn Header="报废状态" Binding="{Binding ScrapStatus}" Width="100"/>
                            <DataGridTextColumn Header="备注" Binding="{Binding ScrapRemarks}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>