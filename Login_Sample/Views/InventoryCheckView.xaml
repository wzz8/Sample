<UserControl x:Class="Login_Sample.Views.InventoryCheckView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Login_Sample.Views"
             xmlns:viewModels="clr-namespace:Login_Sample.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:InventoryCheckViewModel}">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,20,15"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- 页面标题 -->
        <Border Background="White" Height="80" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <materialDesign:PackIcon Kind="ClipboardCheck" Width="24" Height="24" Margin="0,0,10,0"/>
                <TextBlock Text="库存盘点" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
            </StackPanel>
        </Border>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1" Margin="0,0,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 盘点记录信息 -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="盘点记录信息" Style="{StaticResource HeaderTextStyle}"/>

                        <WrapPanel Orientation="Horizontal" IsItemsHost="False">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="盘点记录ID：" Style="{StaticResource LabelTextStyle}"/>
                                <TextBox Text="{Binding CheckRecordId}" Style="{StaticResource TextBoxStyle}" IsReadOnly="True"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="盘点日期：" Style="{StaticResource LabelTextStyle}"/>
                                <TextBox Text="{Binding CheckDate, StringFormat=yyyy-MM-dd HH:mm:ss}" Style="{StaticResource TextBoxStyle}" IsReadOnly="True"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="盘点人：" Style="{StaticResource LabelTextStyle}"/>
                                <TextBox Text="{Binding CheckPerson}" Style="{StaticResource TextBoxStyle}" IsReadOnly="True"/>
                            </StackPanel>
                        </WrapPanel>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,15">
                            <TextBlock Text="盘点备注：" Style="{StaticResource LabelTextStyle}" VerticalAlignment="Top"/>
                            <TextBox Text="{Binding Remarks}" Width="420" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="10" BorderBrush="#BDC3C7" BorderThickness="1"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                            <Button Content="开始盘点" Command="{Binding StartCheckCommand}" Background="#3498DB" Foreground="White" Height="35" Width="100" Margin="0,0,10,0">
                                <Button.Effect>
                                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.2"/>
                                </Button.Effect>
                            </Button>
                            <Button Content="保存盘点结果" Command="{Binding SaveCheckResultCommand}" Background="#2ECC71" Foreground="White" Height="35" Width="120">
                                <Button.Effect>
                                    <DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.2"/>
                                </Button.Effect>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 盘点项列表 -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel Margin="20">
                        <TextBlock Text="库存盘点明细" Style="{StaticResource HeaderTextStyle}"/>

                        <DataGrid ItemsSource="{Binding CheckItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0" Background="White">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="序号" DisplayIndex="0" Width="50" IsReadOnly="True">
                                    <DataGridTextColumn.Binding>
                                        <Binding Path="ItemName"/>
                                    </DataGridTextColumn.Binding>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="配件名称" Binding="{Binding ItemName}" Width="120" IsReadOnly="True"/>
                                <DataGridTextColumn Header="配件编号" Binding="{Binding ItemNumber}" Width="120" IsReadOnly="True"/>
                                <DataGridTextColumn Header="系统库存" Binding="{Binding SystemQuantity}" Width="80" IsReadOnly="True"/>
                                <DataGridTemplateColumn Header="实际盘点数量" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Text="{Binding ActualQuantity, UpdateSourceTrigger=PropertyChanged}" Width="80" Height="30" Padding="5" BorderBrush="#BDC3C7" BorderThickness="1" Margin="0,0,5,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="差异数量" Binding="{Binding Difference}" Width="80" IsReadOnly="True">
                                    <DataGridTextColumn.CellStyle>
                                        <Style TargetType="DataGridCell">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Difference}" Value="0">
                                                    <Setter Property="Foreground" Value="#27AE60"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Difference}" Value="1">
                                                    <Setter Property="Foreground" Value="#E67E22"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Difference}" Value="-1">
                                                    <Setter Property="Foreground" Value="#E74C3C"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.CellStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice, StringFormat=F2}" Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="供应商" Binding="{Binding Supplier}" Width="120" IsReadOnly="True"/>
                                <DataGridTextColumn Header="仓库位置" Binding="{Binding WarehouseLocation}" Width="100" IsReadOnly="True"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- 总差异 -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                            <TextBlock Text="总差异数量：" FontSize="14" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding TotalDifference}" FontSize="16" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#27AE60"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TotalDifference}" Value="1">
                                                <Setter Property="Foreground" Value="#E67E22"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding TotalDifference}" Value="-1">
                                                <Setter Property="Foreground" Value="#E74C3C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>