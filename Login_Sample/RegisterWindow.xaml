<Window x:Class="test.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:test"
        xmlns:vm="clr-namespace:test.ViewModels"
        mc:Ignorable="d"
        Title="注册窗口"
        Height="360"
        Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="{Binding Icon}">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closed">
            <i:InvokeCommandAction Command="{Binding BackToLoginCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>
    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 用户名 -->
        <Label Grid.Row="0" Grid.Column="0" Content="用户名:" Margin="10,30,10,5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,30,20,5" Height="30" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        
        <!-- 邮箱地址 -->
        <Label Grid.Row="1" Grid.Column="0" Content="邮箱地址:" Margin="10,5,10,5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5,20,5" Height="30" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
        
        <!-- 密码 -->
        <Label Grid.Row="2" Grid.Column="0" Content="密码:" Margin="10,5,10,5" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="2" Grid.Column="1" Margin="0,5,20,5" Height="30" vm:PasswordBoxExtensions.PasswordChangedCommand="{Binding PasswordChangedCommand}"/>
        
        <!-- 确认密码 -->
        <Label Grid.Row="3" Grid.Column="0" Content="确认密码:" Margin="10,5,10,5" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="3" Grid.Column="1" Margin="0,5,20,5" Height="30" vm:PasswordBoxExtensions.ConfirmPasswordChangedCommand="{Binding ConfirmPasswordChangedCommand}"/>
        
        <!-- 错误提示 -->
        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Margin="20,5,20,10" Foreground="Red" TextAlignment="Center" FontSize="14" Visibility="{Binding ErrorVisibility}" Text="{Binding ErrorMessage}"/>
        
        <!-- 注册按钮 -->
        <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Content="注册" Margin="20,10,20,5" Height="35" Command="{Binding RegisterCommand}"/>
        
        <!-- 返回登录按钮 -->
        <Button Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Content="返回登录" Margin="20,5,20,20" Height="35" Command="{Binding BackToLoginCommand}"/>
    </Grid>
</Window>